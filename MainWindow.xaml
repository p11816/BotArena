<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525" Closing="WindowClosing">
    <Canvas>
        <Ellipse x:Name="c1" Width="100" Height="100" Fill="Black" Canvas.Top="{Binding X}" Canvas.Left="{Binding Y}"/>
        <Ellipse x:Name="c2" Width="100" Height="100" Fill="Black" Canvas.Top="{Binding X}" Canvas.Left="{Binding Y}"/>
    </Canvas>
</Window>
